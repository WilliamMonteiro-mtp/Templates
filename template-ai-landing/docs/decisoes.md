# Decisões

Produto: `docs/plano.md`. Pessoa: **`conteudo/dados.md`**.

## Fechadas (sistema)

Site completo, slots visíveis, diploma scan nunca, form `mailto:`, deploy Vercel, nota de curso só se `showGrade` em `dados.md`.

## Pessoa (tudo no MD)

Foto, redes, telefone visível, WhatsApp, CV, lacuna, projetos, disponibilidade — campos no `dados.md`, muitos ainda `vazio`.
